[project]
name = "html-tools"
version = "0.1.0"
description = "A set of programs that help you manipulate .html files."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pymarkdownlnt>=0.9.33",
    "ty>=0.0.1a31",
]
